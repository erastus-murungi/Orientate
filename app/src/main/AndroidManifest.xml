<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.erastus.orientate" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".insitutiondashboard.InstitutionDashboardActivity"
            android:label="@string/title_activity_institution_dashboard" >
        </activity>
        <activity
            android:name=".studentdashboard.StudentDashBoardActivity"
            android:label="@string/title_activity_student_dash_board" />
        <activity
            android:name=".home.MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/Theme.MaterialComponents.Light.NoActionBar" />
        <activity
            android:name=".studentsignup.StudentSignUpActivity"
            android:theme="@style/AppTheme.NoActionBar" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>